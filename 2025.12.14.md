### Java:
    集合进阶：      单列集合和双列集合
    Collection(单列集合):在添加元素时有序添加，添加顺序和取出顺序一致。
        迭代器：Iterator
        类Iterator：
        Boolean hasNext();//判断当前位置是否存在元素
        T next();           //获取当前元素并将指针往后移动
        void remove();      //删除最后元素
### Android studio:
    终于把模拟器链接好并打开了模板项目